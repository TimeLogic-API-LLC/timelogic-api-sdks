/* tslint:disable */
/* eslint-disable */
/**
 * TimeLogic API — Direct Customer Access
 * Public direct-access contract for the TimeLogic gateway.  This public spec excludes `/healthz` and the shared clock asset routes. It keeps `/.well-known/time-api-public-key`, `/v1/time/clock`, and signed JSON response controls because public consumers may need them.  Authentication: - direct access supports `Authorization: Bearer <token>`, `X-API-Key`, and `api_key` query credentials - RapidAPI access uses `X-RapidAPI-Key` and `X-RapidAPI-Host`; the SDKs expose this as a `rapidApi` transport option that accepts only the RapidAPI key  Behavioral notes: - all documented operations are `GET` - only one selector family may be used at a time - current and convert support bulk only through one comma-separated `tz`, `ip`, or `offset` selector - add, diff, calendar, dst, elapsed, timezone, and clock are single-target routes - credentials are extracted in Authorization, X-API-Key, then api_key query order; conflicting values are rejected - the first server is the default direct API host. The second server is the RapidAPI gateway and can be selected or overridden by SDK configuration - `sign` is available on supported JSON routes and is not supported on `/v1/time/clock`
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import type { BulkError } from './BulkError';
import {
    instanceOfBulkError,
    BulkErrorFromJSON,
    BulkErrorFromJSONTyped,
    BulkErrorToJSON,
} from './BulkError';
import type { TimePayload } from './TimePayload';
import {
    instanceOfTimePayload,
    TimePayloadFromJSON,
    TimePayloadFromJSONTyped,
    TimePayloadToJSON,
} from './TimePayload';

/**
 * @type TimePayloadBulkItem
 * One bulk array item, either a successful time payload or a per-item bulk error.
 * @export
 */
export type TimePayloadBulkItem = BulkError | TimePayload;

export function TimePayloadBulkItemFromJSON(json: any): TimePayloadBulkItem {
    return TimePayloadBulkItemFromJSONTyped(json, false);
}

export function TimePayloadBulkItemFromJSONTyped(json: any, ignoreDiscriminator: boolean): TimePayloadBulkItem {
    if (json == null) {
        return json;
    }
    if (instanceOfBulkError(json)) {
        return BulkErrorFromJSONTyped(json, true);
    }
    if (instanceOfTimePayload(json)) {
        return TimePayloadFromJSONTyped(json, true);
    }

    return {} as any;
}

export function TimePayloadBulkItemToJSON(json: any): any {
    return TimePayloadBulkItemToJSONTyped(json, false);
}

export function TimePayloadBulkItemToJSONTyped(value?: TimePayloadBulkItem | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    if (instanceOfBulkError(value)) {
        return BulkErrorToJSON(value as BulkError);
    }
    if (instanceOfTimePayload(value)) {
        return TimePayloadToJSON(value as TimePayload);
    }

    return {};
}

