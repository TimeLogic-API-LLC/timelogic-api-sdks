//
// TimePayload.swift
//
// Generated by the TimeLogic API SDK generator
// https://github.com/TimeLogic-API-LLC/timelogic-api-sdks.git
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Canonical single-target time payload. */
public struct TimePayload: Codable, JSONEncodable, Hashable {

    static let dayNumberRule = NumericRule<Int>(minimum: 1, exclusiveMinimum: false, maximum: 7, exclusiveMaximum: false, multipleOf: nil)
    public var unix: Int64
    public var unixMs: Int64
    public var utc: Date
    public var isoLocal: String?
    public var rfc2822: String
    public var human: String
    public var dayNumber: Int
    public var dayShort: String
    public var dayFull: String
    public var timezone: String?
    public var formatted: String?

    public init(unix: Int64, unixMs: Int64, utc: Date, isoLocal: String?, rfc2822: String, human: String, dayNumber: Int, dayShort: String, dayFull: String, timezone: String?, formatted: String? = nil) {
        self.unix = unix
        self.unixMs = unixMs
        self.utc = utc
        self.isoLocal = isoLocal
        self.rfc2822 = rfc2822
        self.human = human
        self.dayNumber = dayNumber
        self.dayShort = dayShort
        self.dayFull = dayFull
        self.timezone = timezone
        self.formatted = formatted
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case unix
        case unixMs = "unix_ms"
        case utc
        case isoLocal = "iso_local"
        case rfc2822
        case human
        case dayNumber = "day_number"
        case dayShort = "day_short"
        case dayFull = "day_full"
        case timezone
        case formatted
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(unix, forKey: .unix)
        try container.encode(unixMs, forKey: .unixMs)
        try container.encode(utc, forKey: .utc)
        try container.encode(isoLocal, forKey: .isoLocal)
        try container.encode(rfc2822, forKey: .rfc2822)
        try container.encode(human, forKey: .human)
        try container.encode(dayNumber, forKey: .dayNumber)
        try container.encode(dayShort, forKey: .dayShort)
        try container.encode(dayFull, forKey: .dayFull)
        try container.encode(timezone, forKey: .timezone)
        try container.encodeIfPresent(formatted, forKey: .formatted)
    }
}

