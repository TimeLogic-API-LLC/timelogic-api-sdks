//
// BulkError.swift
//
// Generated by the TimeLogic API SDK generator
// https://github.com/TimeLogic-API-LLC/timelogic-api-sdks.git
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Per-item error wrapper used inside bulk arrays returned by bulk-capable routes. */
public struct BulkError: Codable, JSONEncodable, Hashable {

    public var error: ApiError
    public var requestId: String
    /** Intentionally omitted from public API error responses to avoid exposing request timing; use request_id for support correlation. */
    public var timestamp: Date?
    public var item: String

    public init(error: ApiError, requestId: String, timestamp: Date? = nil, item: String) {
        self.error = error
        self.requestId = requestId
        self.timestamp = timestamp
        self.item = item
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case error
        case requestId = "request_id"
        case timestamp
        case item
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(error, forKey: .error)
        try container.encode(requestId, forKey: .requestId)
        try container.encodeIfPresent(timestamp, forKey: .timestamp)
        try container.encode(item, forKey: .item)
    }
}

