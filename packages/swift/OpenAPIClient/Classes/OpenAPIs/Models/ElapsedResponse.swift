//
// ElapsedResponse.swift
//
// Generated by the TimeLogic API SDK generator
// https://github.com/TimeLogic-API-LLC/timelogic-api-sdks.git
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct ElapsedResponse: Codable, JSONEncodable, Hashable {

    public enum Direction: String, Codable, CaseIterable {
        case _left = "left"
        case passed = "passed"
    }
    public var seconds: Int
    public var minutes: Int
    public var hours: Int
    public var days: Int
    public var human: String
    /** `left` means the target instant is still in the future. `passed` means the target instant is already in the past. */
    public var direction: Direction
    public var formatted: String?
    public var businessDays: Int?

    public init(seconds: Int, minutes: Int, hours: Int, days: Int, human: String, direction: Direction, formatted: String? = nil, businessDays: Int? = nil) {
        self.seconds = seconds
        self.minutes = minutes
        self.hours = hours
        self.days = days
        self.human = human
        self.direction = direction
        self.formatted = formatted
        self.businessDays = businessDays
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case seconds
        case minutes
        case hours
        case days
        case human
        case direction
        case formatted
        case businessDays = "business_days"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(seconds, forKey: .seconds)
        try container.encode(minutes, forKey: .minutes)
        try container.encode(hours, forKey: .hours)
        try container.encode(days, forKey: .days)
        try container.encode(human, forKey: .human)
        try container.encode(direction, forKey: .direction)
        try container.encodeIfPresent(formatted, forKey: .formatted)
        try container.encodeIfPresent(businessDays, forKey: .businessDays)
    }
}

