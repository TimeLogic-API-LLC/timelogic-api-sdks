//
// DiffResponse.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct DiffResponse: Codable, JSONEncodable, Hashable {

    public enum Direction: String, Codable, CaseIterable {
        case forward = "forward"
        case backward = "backward"
        case same = "same"
    }
    public var seconds: Int
    public var minutes: Int
    public var hours: Int
    public var days: Int
    /** `forward` means `to` is after `from`. `backward` means `to` is before `from`. `same` means both sides resolve to the same instant. */
    public var direction: Direction
    public var human: String
    public var formatted: String?
    public var businessDays: Int?
    public var from: DiffEndpointRef
    public var to: DiffEndpointRef

    public init(seconds: Int, minutes: Int, hours: Int, days: Int, direction: Direction, human: String, formatted: String? = nil, businessDays: Int? = nil, from: DiffEndpointRef, to: DiffEndpointRef) {
        self.seconds = seconds
        self.minutes = minutes
        self.hours = hours
        self.days = days
        self.direction = direction
        self.human = human
        self.formatted = formatted
        self.businessDays = businessDays
        self.from = from
        self.to = to
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case seconds
        case minutes
        case hours
        case days
        case direction
        case human
        case formatted
        case businessDays = "business_days"
        case from
        case to
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(seconds, forKey: .seconds)
        try container.encode(minutes, forKey: .minutes)
        try container.encode(hours, forKey: .hours)
        try container.encode(days, forKey: .days)
        try container.encode(direction, forKey: .direction)
        try container.encode(human, forKey: .human)
        try container.encodeIfPresent(formatted, forKey: .formatted)
        try container.encodeIfPresent(businessDays, forKey: .businessDays)
        try container.encode(from, forKey: .from)
        try container.encode(to, forKey: .to)
    }
}

