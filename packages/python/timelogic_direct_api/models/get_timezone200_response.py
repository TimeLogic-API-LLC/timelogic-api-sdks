# coding: utf-8

"""
    TimeLogic API | A World Time API

    Official public API contract for TimeLogic API.  This public spec excludes `/healthz` and the shared clock asset routes. It keeps `/.well-known/time-api-public-key`, `/v1/time/clock`, and signed JSON response controls because public consumers may need them.  Authentication: - the API supports `Authorization: Bearer <token>`, `X-API-Key`, and `api_key` query credentials - RapidAPI access uses `X-RapidAPI-Key` and `X-RapidAPI-Host`; the SDKs expose this as a `rapidApi` transport option that accepts only the RapidAPI key  Behavioral notes: - all documented operations are `GET` - only one selector family may be used at a time - current and convert support bulk only through one comma-separated `tz`, `ip`, or `offset` selector - add, diff, calendar, dst, elapsed, timezone, and clock are single-target routes - credentials are extracted in Authorization, X-API-Key, then api_key query order; conflicting values are rejected - the first server is the default API host. The second server is the RapidAPI gateway and can be selected or overridden by SDK configuration - `sign` is available on supported JSON routes and is not supported on `/v1/time/clock`

    The version of the OpenAPI document: 1.0.0
    Generated by TimeLogic API SDK generator (https://github.com/TimeLogic-API-LLC/timelogic-api-sdks.git)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import json
import pprint
from pydantic import BaseModel, ConfigDict, Field, StrictStr, ValidationError, field_validator
from typing import Any, List, Optional
from timelogic_direct_api.models.timezone_offset_response import TimezoneOffsetResponse
from timelogic_direct_api.models.timezone_resolved_response import TimezoneResolvedResponse
from pydantic import StrictStr, Field
from typing import Union, List, Set, Optional, Dict
from typing_extensions import Literal, Self

GETTIMEZONE200RESPONSE_ONE_OF_SCHEMAS = ["TimezoneOffsetResponse", "TimezoneResolvedResponse"]

class GetTimezone200Response(BaseModel):
    """
    GetTimezone200Response
    """
    # data type: TimezoneResolvedResponse
    oneof_schema_1_validator: Optional[TimezoneResolvedResponse] = None
    # data type: TimezoneOffsetResponse
    oneof_schema_2_validator: Optional[TimezoneOffsetResponse] = None
    actual_instance: Optional[Union[TimezoneOffsetResponse, TimezoneResolvedResponse]] = None
    one_of_schemas: Set[str] = { "TimezoneOffsetResponse", "TimezoneResolvedResponse" }

    model_config = ConfigDict(
        validate_assignment=True,
        protected_namespaces=(),
    )


    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError("If a position argument is used, only 1 is allowed to set `actual_instance`")
            if kwargs:
                raise ValueError("If a position argument is used, keyword arguments cannot be used.")
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator('actual_instance')
    def actual_instance_must_validate_oneof(cls, v):
        instance = GetTimezone200Response.model_construct()
        error_messages = []
        match = 0
        # validate data type: TimezoneResolvedResponse
        if not isinstance(v, TimezoneResolvedResponse):
            error_messages.append(f"Error! Input type `{type(v)}` is not `TimezoneResolvedResponse`")
        else:
            match += 1
        # validate data type: TimezoneOffsetResponse
        if not isinstance(v, TimezoneOffsetResponse):
            error_messages.append(f"Error! Input type `{type(v)}` is not `TimezoneOffsetResponse`")
        else:
            match += 1
        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when setting `actual_instance` in GetTimezone200Response with oneOf schemas: TimezoneOffsetResponse, TimezoneResolvedResponse. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when setting `actual_instance` in GetTimezone200Response with oneOf schemas: TimezoneOffsetResponse, TimezoneResolvedResponse. Details: " + ", ".join(error_messages))
        else:
            return v

    @classmethod
    def from_dict(cls, obj: Union[str, Dict[str, Any]]) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        error_messages = []
        match = 0

        # deserialize data into TimezoneResolvedResponse
        try:
            instance.actual_instance = TimezoneResolvedResponse.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into TimezoneOffsetResponse
        try:
            instance.actual_instance = TimezoneOffsetResponse.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when deserializing the JSON string into GetTimezone200Response with oneOf schemas: TimezoneOffsetResponse, TimezoneResolvedResponse. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when deserializing the JSON string into GetTimezone200Response with oneOf schemas: TimezoneOffsetResponse, TimezoneResolvedResponse. Details: " + ", ".join(error_messages))
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        if hasattr(self.actual_instance, "to_json") and callable(self.actual_instance.to_json):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(self) -> Optional[Union[Dict[str, Any], TimezoneOffsetResponse, TimezoneResolvedResponse]]:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        if hasattr(self.actual_instance, "to_dict") and callable(self.actual_instance.to_dict):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())


